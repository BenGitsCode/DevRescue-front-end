<tr class="table-top-row">
  <td><b>title</b></td>
  <td><b>Rescue Link</b></td>
  {{!-- <td><b>Tags</b></td> --}}
</tr>
<tr class="table-top-row">
{{#each rescues}}
    <div class="show-rescue">
  {{this.title}}
  <td class="rescue-title">{{this.title}}</td>
  <td class="rescue-url"><a target="_blank" href="{{this.url}}">Rescue Me</a></td>
  <td class="rescue-tag">{{this.tag}}</td> 
  <td><button type="button"  class="btn btn-primary edit-rescue" data-rescue-id="{{id}}" data-toggle="modal" data-target="#NOTedit-rescue-modal">Update</button></td>
  <td><button type="button"  class="btn btn-danger delete-rescue" data-rescue-id="{{id}}" data-toggle="modal" data-target="#delete-rescue-modal">Delete</button></td>
    </div>
</tr>
{{/each}}

  {{#each rescueUrl}}
    <h1>{{this}}</h1>
  {{/each}}
